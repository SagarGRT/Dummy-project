# Test Environment Variables for Portfolio Management Service
# Copy this to .env.test or set these environment variables before running tests
# WARNING: These are test credentials only - do not use in production

API_BASE_URL=https://api.test.example.com
API_KEY=test_api_key_12345
API_SECRET=test_api_secret_67890

# Optional TLS settings for testing
# TLS_CERT_PATH=/path/to/test/cert.pem
# TLS_KEY_PATH=/path/to/test/key.pem 